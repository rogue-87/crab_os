[package]
name = "crab_os"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort" # disable stack unwinding on panic

[profile.release]
panic = "abort" # disable stack unwinding on panic

[dependencies]
bootloader = "0.9"

[[bin]]
name = "crab_os"
test = false
bench = false
